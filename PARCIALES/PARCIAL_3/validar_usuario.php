<?php

session_start();

//creacion de array con usuarios validos
$usuarios[

    'usuario1' => '123';
    'usuario2' => '213';
    'usuario3' => '321';
]


//validacion de usurarios dentro del array
$username = $username(!isset($username)) ? $sesion($username); //falta algo
$password = $password(!isset($password)) ? $sesion($password); //falta algo

if($session($username) && $session($passwords) === $username, $password{
    
    echo("session iniciada con exito");

}
else{
    echo("su usuario o contraseña son incorrectos")
}

 

//este archivo no necesita html, solo es para la creacion y validacion del array